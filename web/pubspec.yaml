name: fair_management_web
description: 58fair backstage management for web
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  dio: ^5.9.2
  cached_network_image: ^3.4.1
  json_annotation: ^4.9.0
  shared_preferences: ^2.5.3
  file_picker: ^10.3.2
  fluttertoast: ^8.2.14

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  json_serializable: ^6.11.1
  build_runner: ^2.7.1

flutter:
  uses-material-design: true
  assets:
    - images/
